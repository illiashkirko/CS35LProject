* Passed prop form board to square. 
   * Square’s number displayed in each square.


	<Square value={i} />


	<button className="square">
         {this.props.value}
  	</button>


* Added Square’s constructor & onClick function.
   * On clicking a square, it displays an X and changes it’s inner state to ‘X’.

	onClick={() => this.setState({value: 'X'}}


	constructor(props) {
   	 	super(props);
    		this.state = {
      	value: null,
    };

* Created an array to store square data in board. Implemented handleClick() function in Board, responds to square's click. 
	*  Same output as before, but now, board holds the square data.

 	this.state = {
      squares: Array(9).fill(null),
    };

    handleClick(i) {
    const squares = this.state.squares.slice();
    squares[i] = 'X';
    this.setState({squares: squares});
  }

* Replaced square class with corresponding function component
	* no change in output

	function Square(props) {
  		return (
    		<button className="square" onClick={props.onClick}>
      	{props.value}
    		</button>
  		);
	}

* Created boolean to determine whose turn it is. Flip boolean each turn. Output turn.
	* Turn status is shown above the board

	xIsNext: true,

	squares[i] = this.state.xIsNext ? 'X' : 'O';

	const status = 'Next player: ' + (this.state.xIsNext ? 'X' : 'O');

* Wrote helper function to declare winner. Checking for winner each turn, changing status and allowing moves accordingly.
	* Winner displayed. Cannot play after win.

    const winner = calculateWinner(this.state.squares);
    let status;
    if (winner) {
      status = 'Winner: ' + winner;
    } else {
      status = 'Next player: ' + (this.state.xIsNext ? 'X' : 'O');
    }


	if (calculateWinner(squares) || squares[i]) {
      return;
    }

*












